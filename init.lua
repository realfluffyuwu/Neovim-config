require("FFPacker") -- Packer Plugins
require("FFTheme")  -- Handles Theme Things
require("FFKeys")   -- Pretty well handles all Keybindings except LSP
require("FFLsp")    -- Handles all the LSP Settings and Keybinds
--require("FFSetup")  -- Handles all the Plugin Setup

-- Have to place this here because Nvim is too Retarded to source it properly
--vim.keymap.set("n", "<leader>m", vim.cmd.Ex)
